# URL de la API en desarrollo. Corresponde al perfil "http" de
# backend/src/Api/Properties/launchSettings.json (http://localhost:5175).
# Se usa HTTP (no HTTPS) para evitar el problema de certificado local no confiable
# en desarrollo (03-arquitectura.md/CLAUDE.md: no modificar el backend para resolver
# un problema exclusivamente del frontend).
VITE_API_BASE_URL=http://localhost:5175
