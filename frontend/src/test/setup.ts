// Ejecutado antes de cada archivo de prueba (vite.config.ts, test.setupFiles). Extiende
// "expect" de Vitest con los matchers de jest-dom (toBeInTheDocument, toHaveTextContent, etc.)
// para pruebas de componentes.
import "@testing-library/jest-dom/vitest";
